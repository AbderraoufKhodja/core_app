import 'package:flutter/material.dart';

class JepitoChat extends StatelessWidget {
  const JepitoChat({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
